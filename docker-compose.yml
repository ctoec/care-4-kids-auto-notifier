version: '2'
services:
  db:
    image: mariadb
    ports:
      - "5432:5432"
    environment: 
      - MYSQL_ROOT_PASSWORD=password
  web:
    build: .
    command: ./dev-start
    ports:
      - "3001:3001"
    links:
      - db
    volumes:
      - .:/app